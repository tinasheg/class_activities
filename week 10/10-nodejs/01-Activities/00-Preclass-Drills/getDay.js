// write a function that takes an integer and returns the corresponding day of the week

var days = ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"]; 

var inputNum = function(num) {
    //if (num < days.length) {
        console.log(days[num - 1])
    // } else { 
    //     console.log("undefined")
    // }
}
inputNum(0);
inputNum(2);
inputNum(9); 

